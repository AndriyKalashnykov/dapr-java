kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: dapr-java
nodes:
  - role: control-plane
    # cloud-provider-kind hands out LoadBalancer IPs on the kind Docker
    # network; services are reachable via that IP directly from the host
    # without extraPortMappings. (Project migrated off MetalLB 2026-04.)
