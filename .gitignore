# Build output
target/

# Java class files
*.class

# Package files
*.jar
*.war
*.ear

# IDE
.idea/
.vscode/
*.iml
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Maven wrapper (if added later)
.mvn/wrapper/maven-wrapper.jar

# Environment / secrets
.env
.env.*
# ...but keep the committed source-of-truth example (see rules/common/configuration.md)
!.env.example

# mermaid-lint scratch outputs
docs/diagrams/out/.mermaid-lint-*.png

# PlantUML version-stamped sentinel — forces re-render on PLANTUML_VERSION
# bump (see Makefile %.png rule). Not part of the committed render output.
docs/diagrams/out/.plantuml-*.stamp

# Claude Code transient state
.claude/scheduled_tasks.lock
.claude/settings.local.json
